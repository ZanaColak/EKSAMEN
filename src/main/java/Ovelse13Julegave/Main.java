package Ovelse13Julegave;

public class Main {
    public static void main(String[] args) {
        JuleGave juleGave = new JuleGave(true, true, true); //Just setting the values to anything, doesn't matter
        juleGave.couldBeLego();
    }
}
